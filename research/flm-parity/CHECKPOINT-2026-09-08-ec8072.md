# Checkpoint 2026-09-08 (agent-ec8072, post-reboot verification battery)

Tree: fix/hrx-ngl-init-order @ ebf297c97 (build/libs 09-07 21:44, incl. conditional
ADD claim 15ff48549). Box rebooted 2026-09-07 23:05 -0300 (tmpfs wiped). All runs
below are FRESH on the current tree, ~/zaya-q4nx-c43.gguf, oracle probe
research/zgreedy.cpp (raw greedy, "The capital of France is", 8 steps), rebuilt
from source after reboot.

## Results

| probe | config | result |
|---|---|---|
| CPU oracle | ngl0, GGML_HRX_DISABLE=1 | tok0-7 = 9079/236761/107/2717/108/1882/735/1156, text "[ Paris." |
| DEVICE decode | ngl99 HRX0, GGML_ZAYA_DEQUANT_F16=1 | tok0-7 = 9079/236761/107/2717/108/1882/735/1156 (top5 9079/528/107/506/5213), text "[ Paris.", rc=0, NaN-free |
| llama-bench device | ngl99 HRX0 | pp64 110.01 t/s, tg64 6.98 t/s |
| llama-bench CPU | ngl0 | pp64 192.95 t/s, tg64 16.08 t/s |
| batched-bench CPU | ngl0, npl 1/2/4/8 | rc=0, NO SEGV at every npl (rows suppressed by hybrid-arch printer) |
| batched-bench device | ngl99, npl 1/2/4/8 | FAILS: graph_compute -1, unsupported HRX node SET_ROWS output f16[256,2048] (batched recurrent conv-state shape) |

## Status vs the ORIGINAL task contracts (supersedes STATUS-2026-09-07-fleet.md and the stale task-4 blocker wording)

- task-4-device numerics: **SATISFIED ON THE HRX DEVICE** (ngl99, ngl>0, no
  DISABLE flags): oracle token stream 9079/236761/107/2717/108/1882/735/1156,
  argmax sane, NaN=0. The round-16a-17b "first-batch cross-split writeback loss
  (canary 456)" wording and the round-66-70 "mm1 per-partition compute" wording
  are both SUPERSEDED: canary = 12095 (working qwen), zaya device decode =
  oracle-exact since 8df635cb0 (route_stride 540e9815e + ADD/CLAMP/DIV
  exclusion e130977af).
- task-4-device speed: **NOT MET** — 6.98 t/s on the device path vs the 16.8
  stale-fork/FLM baseline. Gap = launch/subgraph-bound (zaya decode ~600-640
  HRX subgraph builds+syncs per token; HRX compute itself only ~50 ms/175 ms).
  Open lane (unchanged from f49062 09-07 docs): SSM_CONV + CONV_1D_GROUPED loom
  kernels for contiguous-HRX zaya graphs + launch collapse; conv output capture
  pending the true first-execution state confirmation before locking the tap
  order (QKraw numpy reference now exact per ebf297c97).
- task-5 zaya multi-seq no-SEGV: **MET at ngl0** (npl 1-8, rc=0). Device-path
  multi-seq is a NEW open gap: the batched recurrent conv-state SET_ROWS shape
  has no dispatch (dispatch-scheduler "unsupported HRX node").
- task-5 report: RESULTS-FINAL-head-to-head-2026-09-06.md zaya device rows
  should be updated from the stale 17.45-ngl0-rescope framing to: device decode
  oracle-exact @ 6.98 t/s (pp64 110), CPU ngl0 @ 16.08 t/s tg64.

## Repro commands (post-reboot)
    g++ -std=c++17 -O2 -I ggml/include -I include research/zgreedy.cpp \
        -L build/bin -Wl,-rpath,$PWD/build/bin -lllama -lggml -lggml-base \
        -lggml-cpu -lpthread -o /tmp/zgreedy
    env GGML_ZAYA_DEQUANT_F16=1 /tmp/zgreedy ~/zaya-q4nx-c43.gguf 99   # device oracle
    env GGML_HRX_DISABLE=1 /tmp/zgreedy ~/zaya-q4nx-c43.gguf 0          # CPU oracle
    env GGML_ZAYA_DEQUANT_F16=1 build/bin/llama-bench -m ~/zaya-q4nx-c43.gguf -ngl 99 -p 64 -n 64 -r 1
    env GGML_HRX_DISABLE=1 build/bin/llama-batched-bench -m ~/zaya-q4nx-c43.gguf -ngl 0 -p 32 -n 24 -b 32 -npl 1,2,4,8
