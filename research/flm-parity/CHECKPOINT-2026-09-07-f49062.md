# Checkpoint re-validation (2026-09-07 00:36 UTC, agent-f49062)

Fresh re-run on the current tree (fix/hrx-ngl-init-order @ cffc19e18 + b30173
uncommitted WIP in tree) to re-establish ground truth after the fleet
consolidation. Box: strixhalo. Oracle probe = research/zgreedy.cpp (raw greedy
decode, prompt "The capital of France is", 8 steps), rebuilt from source.

## CPU oracle — RE-VERIFIED (GGML_HRX_DISABLE=1, ngl 0)
tok0..7 = 9079/236761/107/2717/108/1882/735/1156, text "[ Paris." — matches the
task-4 contract oracle exactly (top5 step0: 9079/528/107/5213/506).

## HRX device decode (ngl 99, GGML_ZAYA_DEQUANT_F16=1) — STILL DIVERGES
tok0..7 = 563/1456/531/16239/506/2608/236761/669, text "[ is used to hide the
problem. The]" = the round-63 baseline divergence variant. EXIT=0, no hang, no
NaN (log /tmp/zgreedy-dev99.log, 1887 lines).
=> The b30173 WIP (12:58 loom fragment/motif edits + dispatch-mul-mat-id debug
buffer + loom-jit GGML_HRX_DUMP_IR + mma_repro op) does NOT fix the gate_up
mm t1-5 divergence. Round-69/70 root cause stands: mm1 (ffn_moe_gate_up
MUL_MAT_ID) per-partition compute wrong for prefill tokens 1-5 (t0 correct mad
0.005; downstream cascade 1.0 -> 0.4 -> 0.15 as documented). WIP is diagnostic
scaffolding + an ineffective fix candidate; fix still OPEN at the mm1
fragment-path/codegen level.

## Note on the stale task-4 blocker wording
Goal metadata for task-4-device still says "execution-side first-batch
cross-split writeback loss (canary 456 vs 12095) ... Needs the iree/amdgpu
device-layer owner". Rounds 64-70 SUPERSEDED that: the readback corruption
(canary divergence class) was fixed by 69f0fceb9 (strided buffer_get ->
extent convention); the remaining divergence = a REAL compute bug in the
ggml-hrx mul_mat_id wmma kernel (per-partition, tokens 1-5), i.e. our own
kernel code, not the iree/amdgpu device layer.

## Next (unchanged from fleet status)
Land the mm1 per-partition compute fix; re-run this exact probe until
tok0..7 == oracle; then task-4 benches (single-seq >= 16.8 t/s) and task-5
(zaya multi-seq npl 1-8 + final head-to-head rows).

## Addendum (00:5x UTC): permutation finding - see permutation-finding/00-FINDING.md
